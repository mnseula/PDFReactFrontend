version: '3.8'

services:
  pdf-processor-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pdf-processor-app
    volumes:
      - ./web-build:/app/web-build
    ports:
      - "8081:8081"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
